services:
  - type: web
    name: omninumerology-api
    runtime: node
    buildCommand: npm ci --include=dev && npm run build:server
    startCommand: npm run start:prod
    healthCheckPath: /api/health
    autoDeploy: true
